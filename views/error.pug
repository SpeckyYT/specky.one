doctype html

include includes/style.pug

html(lang="en")
    head
        title Login Failed
    body
        h1 #{code || 400} - #{title || "Error"}
        p(style="color: red") #{error || "Error"}
        a(href="./") How about you go back
    include includes/footer.pug
